const {zaynab, hammouda } = require('./names');
const sayHi = require('./utils');

const alter = require('./alternative');

require('./multiplier');

console.log(alter);

sayHi('Mariam');
sayHi(zaynab);
sayHi(hammouda);